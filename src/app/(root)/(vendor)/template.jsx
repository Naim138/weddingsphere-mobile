import React from 'react'

const VendorTemplate = ({children}) => {

    // condition to check valid vendor can access
  return (
    <>
            {children}
    </>
  )
}

export default VendorTemplate